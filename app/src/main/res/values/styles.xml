<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base application theme -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.DarkActionBar">
        <!-- Primary colors (main app color) -->
        <item name="colorPrimary">@color/blue_primary</item>
        <item name="colorPrimaryVariant">@color/blue_primary_dark</item>
        <item name="colorOnPrimary">@color/white</item>

        <!-- Secondary colors (accent elements) -->
        <item name="colorSecondary">@color/blue_accent</item>
        <item name="colorSecondaryVariant">@color/blue_light</item>
        <item name="colorOnSecondary">@color/white</item>

        <!-- This is the key one that controls most UI elements -->
        <item name="colorAccent">@color/blue_primary</item>

        <!-- Controls many Material component colors -->
        <item name="android:colorControlActivated">@color/blue_primary</item>
        <item name="android:colorControlHighlight">@color/blue_light</item>

        <!-- Status bar -->
        <item name="android:statusBarColor">@color/blue_primary_dark</item>

    </style>

    <style name="CustomDatePickerTheme" parent="ThemeOverlay.MaterialComponents.MaterialCalendar">
        <item name="colorPrimary">@color/blue_primary</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorSecondary">@color/blue_accent</item>
        <item name="android:colorControlActivated">@color/blue_primary</item>
    </style>

    <style name="CustomTimePickerTheme" parent="ThemeOverlay.MaterialComponents.TimePicker">
        <item name="colorPrimary">@color/blue_primary</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorSecondary">@color/blue_accent</item>
        <item name="colorOnSecondary">@color/white</item>
        <item name="android:colorControlActivated">@color/blue_primary</item>
        <item name="android:colorControlHighlight">@color/blue_primary_light</item>
    </style>




</resources>
